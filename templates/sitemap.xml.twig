<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
	{% for route, frequency in pages %}
		<url>
			<loc>{{ url(route) }}</loc>
			<changefreq>{{ frequency|e }}</changefreq>
		</url>
	{% endfor %}

</urlset>
